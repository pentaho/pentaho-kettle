# optional (unless used in your yaml data below) - can be overridden by upstream properties using the same name
buildProperties:
        # global build control defaults
        # These are understood by the shared pipeline libraries:
        BUILD_PLAN_ID                  : Insane Build Data Test
        MAVEN_DEFAULT_DIRECTIVES       : clean install
        ANT_DEFAULT_DIRECTIVES         : clean-all resolve publish
        GRADLE_DEFAULT_DIRECTIVES      : -q
        
        # job item field defaults
        # These are arbitrary properties for doing replacements in this data file
        SCM_HOST_ROOT                  : https://github.com/
        PENTAHO_SCM_ROOT               : https://github.com/pentaho/
        WEBDETAILS_SCM_ROOT            : https://github.com/webdetails/
        DEFAULT_BRANCH                 : master
        
jobGroups:
  # Always required

  group100:

     - jobID           :  clear-build-caches
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  NA
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  delete-build-output-artifacts
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE
       
  group200:

     - jobID           :  coding-standards
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-coding-standards.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  coding-standards.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE
       
  group300:

     - jobID           :  parent-poms
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/maven-parent-poms.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group400:

     - jobID           :  parent-poms-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/maven-parent-poms-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Dobfuscate-version-for-license-phase=none clean deploy
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group500:

     - jobID           :  metastore
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/metastore.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  metastore.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  osgi-bundles-lowdeps
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-osgi-bundles.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-P lowdeps -DskipDefault -DskipTests clean deploy
       versionProperty :  pentaho-osgi-bundles.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  oss-license-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/oss-licenses.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  oss-licenses.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group600:

     - jobID           :  database-model
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-commons-database.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-database-model.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  mongo-utils
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-mongo-utils.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-mongo-utils.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  eula-wrap-config
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-eula-wrap-config.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Dobfuscate-version-for-license-phase=none clean deploy
       versionProperty :  pentaho-eula-wrap-config.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group700:

     - jobID           :  reporting-libraries
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Dlibraries -DskipTests -Djavadoc clean deploy
       versionProperty :  pentaho-reporting.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  gwt-widgets
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-commons-gwt-modules.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  commons-gwt.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  connections
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-connections.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-connections.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  commons-json
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-commons-json.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-json.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  application-launcher
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-application-launcher.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-launcher.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  version-checker
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-versionchecker.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-versionchecker.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  hdfs-vfs
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-hdfs-vfs.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-hdfs-vfs.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  s3-vfs
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-s3-vfs.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-s3-vfs.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  apache-vfs-browser
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/apache-vfs-browser.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-vfs-browser.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group800:

     - jobID           :  xul
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-commons-xul.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-xul.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  actionsequence-dom
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-actionsequence-dom.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-actionsequence-dom.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group900:

     - jobID           :  metadata-registry
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-registry.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-registry.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1000:

     - jobID           :  mondrian-jar
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/mondrian.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-P !workbench -P !assemblies -Djavadoc clean deploy -DskipTests
       versionProperty :  mondrian.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1100:

     - jobID           :  pdi-engine-core
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-kettle.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Djavadoc -DskipDefault -P base|plugins|lowdeps clean deploy
       versionProperty :  pdi.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1200:

     - jobID           :  metadata
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-metadata.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-metadata.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1300:

     - jobID           :  database-gwt
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-commons-database.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-database-model.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  vertica-bulkloader
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-vertica-bulkloader.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  vertica-bulkloader-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-teradata-tpt-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-teradata-tpt-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-teradata-tpt-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  chartbeans
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-chartbeans.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-chartbeans.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  license-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee-license.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-ee-dsc.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-dataservice-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-dataservice-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-dataservice-client-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1400:

     - jobID           :  big-data-hadoop-shims
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-hadoop-shims.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -DskipDefault -Deula-wrap -P api|shims|all clean deploy
       versionProperty :  pentaho-hadoop-shims.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1500:

     - jobID           :  big-data-splunk-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-splunk-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-splunk-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  big-data-mongodb-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-mongodb-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-mongodb-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  big-data-cassandra-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-cassandra-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-cassandra-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1600:

     - jobID           :  mondrian-workbench
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/mondrian.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-P !mondrian -P !assemblies clean deploy -DskipTests
       versionProperty :  mondrian.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1700:

     - jobID           :  mondrian-schema-workbench-plugins
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-mondrianschemaworkbench-plugins.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-mondrianschemaworkbench-plugins.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1800:

     - jobID           :  psw-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/mondrian.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-P !mondrian -P !workbench clean deploy -DskipTests
       versionProperty :  mondrian.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group1900:

     - jobID           :  psw-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-schema-workbench-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=clean deploy -DskipTests -Deula-wrap
       versionProperty :  mondrian.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  reporting-engine
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Dengine -DskipTests -Djavadoc clean deploy
       versionProperty :  pentaho-reporting.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2000:

     - jobID           :  reportwizard-core
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reportwizard-core.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-report-wizard-core.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2100:

     - jobID           :  platform-base
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipDefault -P platform-base -DskipTests -Djavadoc clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2200:

     - jobID           :  osgi-bundles-highdeps
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-osgi-bundles.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-P highdeps -DskipDefault -DskipTests clean deploy
       versionProperty :  pentaho-osgi-bundles.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  mql-editor
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/mql-editor.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-mql-editor.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-palo-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-palo-core.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-palo-core.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2300:

     - jobID           :  pdi-osgi-bridge
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-osgi-bridge.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-osgi-bridge.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  metaverse-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-metaverse.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-metaverse.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2400:

     - jobID           :  pdi-core-plugins
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-kettle.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -DskipDefault -P plugins|highdeps clean deploy
       versionProperty :  pdi.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-monitoring-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-monitoring-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  pdi-monitoring-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  modeler
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/modeler.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-modeler.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cpf-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cpf.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-cpf-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  ccc-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/ccc.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve dist dist-source
       versionProperty :  pentaho-ccc-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2500:

     - jobID           :  camel-components
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-camel-components.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-camel.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE
       

     - jobID           :  pdi-dataservice-server-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-dataservice-server-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-dataservice-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  reporting-specials
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Dspecials -DskipTests clean deploy
       versionProperty :  pentaho-reporting.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-platform-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-platform-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-pdi-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-platform-utils-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-platform-utils-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-platform-utils-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-plugin-jpivot
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-jpivot.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-jpivot-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cdf-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cdf.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-cdf-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2600:

     - jobID           :  common-ui
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-common-ui.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  common-ui.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2700:

     - jobID           :  data-profiling-ce
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-data-profiling.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-data-profiling.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  data-access-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/data-access.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-data-access.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  marketplace-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/marketplace.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-marketplace-modules.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-user-console
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipDefault -P platform-user-console -DskipTests -Djavadoc clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  big-data-plugin-features
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/big-data-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DfeaturesOnly -DskipTests clean deploy
       versionProperty :  big-data-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-data-refinery-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-data-refinery.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-data-refinery-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2800:

     - jobID           :  pdi-daemon
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  ael.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cde-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cde.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-cde-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cpk-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cpk.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho-cpk-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cgg-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cgg.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=dist dist-source
       versionProperty :  pentaho-cgg-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group2900:

     - jobID           :  sparkl-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/sparkl.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve dist dist-source
       versionProperty :  pentaho-sparkl-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-reporting-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-reporting-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3000:

     - jobID           :  big-data-hadoop-shims-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-big-data-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests -DskipDefault -P base|all clean deploy
       versionProperty :  pentaho-hadoop-shims.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  cda-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}webdetails/cda.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-cda-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3100:

     - jobID           :  jdbc-distribution-utility
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/jdbc-distribution-utility.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  jdbc-distribution-utility.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  dashboards-chart-editor-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-dashboard-chart-editor.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve gwt-compile jar obfuscate dist dist-source
       versionProperty :  pentaho-dashboard-chart-editor.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3200:

     - jobID           :  platform-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipDefault -P platform-ee -DskipTests clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3300:

     - jobID           :  geo-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-geo.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  pentaho-geo-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3400:

     - jobID           :  analyzer-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-analyzer.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Dagilebi -Deula-wrap  -DskipTests clean deploy
       versionProperty :  analyzer-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3500:

     - jobID           :  platform-chart-plugin-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee-chart-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve jar obfuscate dist-source package
       versionProperty :  pentaho-ee-chart-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  mobile-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-mobile.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve jar obfuscate dist dist-source package
       versionProperty :  mobile-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  analysis-plugin-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-analysis-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  analysis-ee-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-yarn-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-yarn-kettle-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-kettle-yarn-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  data-exploration
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-det.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-det.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3600:

     - jobID           :  data-exploration-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-det-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-det.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  karaf-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-karaf-assembly.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-karaf.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  dashboards-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-dashboards.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve gwt-compile jar obfuscate dist dist-source package
       versionProperty :  dashboard-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-weka-plugins
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-data-mining.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-data-mining.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-plugins-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3700:

     - jobID           :  pdi-weka-plugins-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-data-mining-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-data-mining.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  big-data-plugin-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/big-data-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  big-data-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pmr-vizor-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-pmr-vizor.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve jar obfuscate dist-source package
       versionProperty :  pmr-vizor-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pir-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-plugin-interactive-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests clean deploy
       versionProperty :  pir-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-scheduler-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-scheduler-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-scheduler-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-google-analytics-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-google-analytics-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve jar obfuscate dist-source package
       versionProperty :  pdi-google-analytics-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-google-docs-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-google-docs-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-google-docs-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-jms-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-jms-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-jms-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-sdk-plugins
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-sdk-plugins.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests clean deploy
       versionProperty :  pdi-sdk-plugins.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-r-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-r-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve jar obfuscate dist-source package
       versionProperty :  pdi-r-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-sap-hana-bulk-loader-plugin
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-sap-hana-bulk-loader-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  pdi-sap-hana-bulk-loader-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3800:

     - jobID           :  karaf-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-karaf-ee-assembly.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-karaf.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group3900:

     - jobID           :  big-data-plugin-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-big-data-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests -DskipDefault -P assemblies|hdp26|cdh512|mapr520 clean deploy
       versionProperty :  big-data-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  prd-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reporting.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Ddesigner -DskipTests -Djavadoc clean deploy
       versionProperty :  pentaho-reporting.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4000:

     - jobID           :  prd-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-reportdesigner-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests clean deploy
       versionProperty :  pentaho-reporting.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pdi-pur-plugin-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-ee-plugin.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pdi-pur-plugin.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4100:

     - jobID           :  pdi-operations-mart-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pdi-operations-mart.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  pentaho-operations-mart.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4200:

     - jobID           :  pdi-client-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-kettle.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -DskipDefault -P assemblies clean deploy
       versionProperty :  pdi.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4300:

     - jobID           :  pdi-client-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  RELEASE_MODE=false&BUILD_TARGET=clean-all resolve resolve-obf di-package
       versionProperty :  pdi.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4400:

     - jobID           :  metadata-editor
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-metadata-editor.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  pentaho-metadata.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-server-assembly
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipDefault -P platform-assemble -DskipTests clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pad
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-aggdesigner.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests clean deploy
       versionProperty :  aggdesigner.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4500:

     - jobID           :  foundry-workers-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipTests -Deula-wrap clean deploy
       versionProperty :  worker-nodes.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-server-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-platform-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-DskipDefault -P platform-ee-assemble -Deula-wrap -DskipTests clean deploy
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  metadata-editor-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-metadata-editor-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all resolve package
       versionProperty :  pentaho-metadata.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  pad-assembly-ee
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-aggdesigner-ee.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  MAVEN_GOALS=-Deula-wrap -DskipTests clean deploy
       versionProperty :  aggdesigner.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  platform-sdk
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-sdk.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=clean-all assemble
       versionProperty :  pdi-sdk-plugins.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4600:

     - jobID           :  installer-upgrade-utility
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  platform.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4700:

     - jobID           :  installer-bisuite
       executeFlag     :  true
       scmUrl          :  ${SCM_HOST_ROOT}pentaho/pentaho-installer.git
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :  BUILD_TARGET=assemble-x64-bisuite-windows-installer assemble-x64-bisuite-linux-installer assemble-x64-bisuite-mac-installer
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4800:

     - jobID           :  harvester
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

  group4900:

     - jobID           :  engops/remote-ip-job-trigger
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  NA
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  engops/regression-smoketest-job-trigger
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  NA
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  snapshot-checker
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  NA
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE

     - jobID           :  obfuscation-verifier-job-trigger
       executeFlag     :  true
       scmUrl          :  null
       scmBranch       :  ${DEFAULT_BRANCH}
       buildFramework  :  maven
       directives      :
       versionProperty :  pentaho.version
       buildFile       :  pom.xml
       # optional - defaults to project root
       root            :
       # optional: defaults to auto (one of: AUTO, FORCE, or NOOP)
       executionType   :  FORCE
