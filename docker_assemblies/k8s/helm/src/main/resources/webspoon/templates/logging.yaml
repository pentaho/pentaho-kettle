apiVersion: logging.hitachivantara.com/v1
kind: LoggingSolution
metadata:
  name: {{ template "webspoon.fullname" . }}-ls
spec:
  releaseName: {{ .Release.Name }}
  flows:
    - selectors:
        app.kubernetes.io/instance: {{ .Release.Name }}
