# meta-inject-p - Pentaho Meta Inject plugin
Allows Metadata Injection in PDI

## Building
Maven is used to build the project, simply run the following command to compile and package it:

	mvn package
